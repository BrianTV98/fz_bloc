import 'package:flutter/material.dart';

class LoadSuccessWidget extends StatelessWidget {
  const LoadSuccessWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}